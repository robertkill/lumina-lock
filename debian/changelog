lumina-lock (0.4.13) unstable; urgency=medium

  * Added the session power menu — what dde-lock provides as
    org.deepin.dde.ShutdownFront1. The dock's power button, the launcher's and
    any session-issued request reach it, and its .service file activates this
    same binary, so the process that owns the lock owns this too.
    The method and signal names are dde-lock's, so nothing that calls it can
    tell the difference; only the surface is new, and it is not a copy of the
    original's grid of tiles: a vertical stack of rows over a two-tone gradient
    that drifts while the menu is open, the selection carried by a marker that
    changes height and a chevron that eases in, the rows arriving one after
    another, and destructive rows that fill for a beat before acting — moving
    away or pressing Esc takes the arm back.
  * Availability is asked for rather than assumed: CanShutdown, CanReboot,
    CanLogout, CanSuspend and CanHibernate decide which rows are live, and the
    two update rows (UpdateAndShutdown / UpdateAndReboot, which hand the request
    to lastore's PrepareFullScreenUpgrade so the update daemon runs the upgrade
    and brings the machine down itself) are dimmed with a reason when there is
    no update daemon to hand them to.
  * Fixed a keyboard bug that this work exposed. ScreenManager reroutes a key
    press to whichever screen the pointer is on, and treated "no screen claimed
    yet" as another screen, so it consumed the key. With the power menu up, every
    key aimed at it was swallowed whenever the pointer sat anywhere but the auth
    screen — the normal case on a multi-screen desk. The menu now takes the
    keyboard for as long as it is up.
  * LUMINA_POWER_DRY_RUN=1 makes the menu log the action instead of performing
    it. It can only ever prevent an action, so it is harmless to ship.
  * Removed the jagged edges. Qt Quick draws rounded corners and scaled images
    without smoothing unless asked, and the panels that were already smooth
    (GlassPanel, and so the password pill) are the ones that had been asked:
    the avatar's circle and its mask, the scaled account picture, and the menu's
    rows, arm fill and marker were not. All of them now set antialiasing or
    smooth as appropriate.
  * Show the account's own picture instead of always drawing the initial. It
    comes from the same place dde-lock takes it: IconFile on the accounts service
    (system bus, /org/deepin/dde/Accounts1/User<uid>, interface
    org.deepin.dde.Accounts1.User), and it is used only when the file exists and
    is not empty — otherwise the initial stands, exactly as the reference does.
    Fetched asynchronously, because this is the lock's startup path.
    Two mistakes on the way there, both caught by running it rather than reading
    it: asking asyncCall for "org.freedesktop.DBus.Properties.Get" as a method on
    the *user* interface builds an invalid member name and libdbus aborts the
    process (the lock would have crashed on startup); and Properties.Get answers
    with a variant that Qt may already have unwrapped, so unwrapping
    unconditionally turned the path into nothing. The same unwrap bug was in the
    lastore CheckUpdateMode read, which would have handed the update daemon mode
    0.
  * Made the password character bigger (16 * unit -> 22 * unit) and put the
    border weight back to 1 * unit, which is where it started — an earlier
    "1.4 * unit" change never applied (the script that carried it aborted on its
    first edit), so the borders had stayed at 2 * unit throughout.
  * Removed the jagged edges where it is possible without new assets. Qt Quick
    leaves rounded corners and scaled images unsmoothed unless asked, and the
    panels that were already smooth (GlassPanel, and with it the password pill)
    are the ones that had been asked. The avatar's circle and mask, the scaled
    account picture, and the menu's rows, arm fill and marker now ask too.
    One edge remains stepped: the outside of a *picture* avatar, because Qt does
    not antialias a rounded Rectangle when it is rendered into a layer and a
    layer is what the mask has to be. Doing better needs either a mask image with
    the soft edge baked in, or a precompiled .qsb fragment shader (Qt 6 rejects
    inline GLSL — fragmentShader is a URL to a .qsb, and this project ships none).
  * Thickened the edges that are aimed at: the menu rows and the password pill
    now use a 2 * unit border instead of a hairline. Both scale with the screen
    like everything else, and the shutdown row's colour needs the weight to read
    as an edge rather than a tint.
  * Gave the menu an exit. It faded in and then cut: the scene gated its own
    visibility on "is it up", so on the frame the exit began the item vanished,
    and the window was hidden at the same instant. The scene now stays visible
    while it is leaving, and the window is hidden only once the exit has played —
    the scene reports back, with a 1.5s fallback because a window left up would
    hold the keyboard with nothing on screen. Measured across the exit, the
    screen goes 44 -> 41 -> 37 -> 29 -> 20 -> 13 -> 0: a decay, not a step.
  * Clicking past the rows dismisses the menu and returns to the desktop. The
    rows take their own clicks, so only the space around them is affected.
  * The shutdown row carries the danger colour on its border, selected or not:
    it is the one action that cannot be taken back.
  * Declared both surfaces' window flags in QML instead of setting them from C++
    after the window already existed. Setting a window flag makes Qt destroy and
    recreate the native window, so whether the window manager had already decided
    to manage it depended on how that recreation lined up with the mapping — the
    open animation appeared only some of the time. Declared up front there is
    nothing to race: xwininfo reports Override Redirect State: yes for the menu
    on its first showing and on every reopening, and for the lock window, at the
    screen's full size. showAll() now logs whether the flag is in place at the
    moment the window is mapped (bypassWM=true), which is the property that used
    to be timing-dependent.
  * Fixed the two update rows doing nothing. They addressed lastore under its
    classic name, com.deepin.lastore, which does not exist in the 6.x generation:
    the availability probe therefore answered "no update daemon" and both rows
    stayed dimmed, so clicking them could not do anything at all. They now use
    org.deepin.dde.Lastore1 / /org/deepin/dde/Lastore1 / ...Lastore1.Manager, and
    the probe reports updatesAvailable=true against the machine this was reported
    from. The show log carries that flag, so it can be confirmed at a glance.
  * Made the confirmation countdown restart rather than resume. Cancelling with
    Esc and arming the same row again continued from wherever the cancelled fill
    had stopped, because the fill's width was animated and left part-way: the
    second press could act almost immediately. A fresh arm now resets the
    progress to zero before counting. Measured: not fired 0.9s after re-arming,
    fired 1.4s after it.
  * Fixed the empty window left over the desktop after the first unlock — the
    lock's own long-standing bug, not something the menu introduced, and the
    reason the menu used to draw nothing when it borrowed those surfaces.
    Both the lock's window and the menu's declared `visibility: Window.FullScreen`
    in QML, which states what the window wants to be and fights the C++ that
    hides it: after an unlock the lock's window stayed mapped over the desktop
    (visible in the taskbar, clearable only with Alt+F4) and clicks never reached
    the desktop. Visibility is now owned by ScreenManager alone. Proven by the
    new logging, which reads the state *after* the hide: `hideAll after
    visible=false visibility=0`.
  * The menu's window is override-redirect on X11 — the same treatment the lock's
    own windows get — so the window manager neither animates it open nor lists
    it, and it is transparent, so the menu fades in out of the desktop rather
    than opening as a window.
  * The menu now draws in a window of its own instead of as a layer on the lock's
    surfaces. The lock's windows are hidden and re-shown around every lock and
    unlock, and this package's own logging showed the failure mode that follows
    from borrowing them: the menu reached a fully settled scene (shown, opacity
    1.0, correct size, eight rows) and nothing appeared, because the window it
    was drawing into had been left in a state the window manager would not
    present — after an unlock a surface stayed mapped over the desktop (visible
    in the taskbar, cleared only with Alt+F4). A window that only ever shows the
    menu has nothing else to go wrong, and the lock's window lifetime is no
    longer on the path at all.
    The window is sized from its screen the way the lock's windows are, because
    a window that is merely asked to go fullscreen keeps the geometry it was
    created with until the window manager answers — the first attempt drew the
    menu into a 160x160 corner.
  * Fixed two problems the first hands-on test of the menu turned up.
    - It blocked the GUI thread — and held the keyboard grab — while asking the
      session manager which actions were available. Those Can* calls are
      synchronous and ran inside a D-Bus method handler, so a session manager
      that was slow to answer, or that was itself waiting on our reply,
      deadlocked: the page never appeared and the session had no keyboard, which
      is indistinguishable from the lock having taken the machine hostage.
      Against a session manager that answers in 3s, Show() took 15.0s and is now
      measured at 0.01s. Availability is cached and refreshed asynchronously, and
      lastore's CheckUpdateMode is read asynchronously as well.
    - Shown over an unlocked session it flashed the lock's own clock before the
      menu: bringing the borrowed surfaces up starts the lock's entry reveal.
      Over an unlocked session the menu now covers the screen on its first frame
      and fades nothing; the rows keep their stagger, which happens over an
      already-opaque cover.

 -- Lumina <lumina@example.com>  Wed, 17 Sep 2026 15:10:00 +0800

lumina-lock (0.4.12) unstable; urgency=medium

  * Multi-screen: the authentication UI now follows the user instead of being
    pinned to the primary screen. Every screen runs the same surface, and
    exactly one of them is "active" at a time — it compacts the clock and
    reveals the password field, while the others stay in Idle (wallpaper,
    clock, hint) and can take the lock over at any moment.

    Which screen that is follows the interaction, like macOS: a click gives it
    to the screen that was clicked, and a key press gives it to the screen the
    pointer is on. The pointer is the only usable signal for the keyboard case,
    because the X11 grab delivers every keystroke to one window — the one
    holding the grab — which is not necessarily where the user is looking.
    ScreenManager therefore watches key presses application-wide: when a
    keystroke arrives for a screen other than the active one, the interactive UI
    moves there and the keystroke that woke it becomes the first character of
    that screen's password, instead of being typed into a field the user cannot
    see. The keyboard grab moves with the field, since the grab window is what
    receives the keys.

    The pointer is deliberately *not* grabbed any more. With one fullscreen
    window per screen the clicks already land on the lock, and an X11 pointer
    grab funnels every click into the grab window — which is exactly what would
    stop "click the screen you are looking at" from working. Alt+Tab / Super
    blocking is unaffected: that is the keyboard grab.

    This also removes a lock-out: unplugging or disabling the primary screen
    while locked used to destroy the only screen with a password field and
    release the input grab, leaving a lock with nothing to type into. The
    remaining screens can now always be woken, and when the active screen
    disappears the choice simply falls back.

  * SecondaryScreen.qml is gone: every screen runs LockScreen.qml, so a video
    wallpaper now decodes on every screen rather than only on the primary one
    (the decoder is still released while the lock is hidden). WallpaperHost
    drops its `playVideo` switch, which had no caller left.

  * Multi-screen smoke test in the README (Xvfb + xdotool), and the D-Bus
    smoke test still covers the lockFront surface.

 -- Lumina <lumina@example.com>  Wed, 16 Sep 2026 12:00:00 +0800

lumina-lock (0.4.11-1) unstable; urgency=medium

  * Restored the clock's entry growth, this time on purpose. Removing the
    accidental growth (the one that ran from zero) had also removed any size
    change from the entry, leaving only the fade. The type size is now a pure
    function of `unit` and two dimensionless progress values, `entryProgress`
    and `compactProgress`; only the progress values animate, and nothing
    animates a pixel size. A Behavior on the size can express neither: with the
    window sized after the first layout, an always-on Behavior animated the
    type out of nothing, and a Behavior gated by the reveal snapped instead,
    because the gate and the value it guards are released by the same signal —
    Qt assigns the new size before the gate opens. The clock now arrives at
    82% and settles over 560 ms, slightly longer than the 480 ms fade, so the
    growth is still in progress while the clock is visible. Measured: the
    glyph run grows about 10% across the visible part of the entry.

  * Fixed the wallpaper jumping from the flat cover colour to the video, and
    restructured how the wallpaper reveals itself. The cover used to sit
    underneath the video, so the video could only ever appear on top of it in
    a single frame. Every layer is now revealed by fading the layer above it
    out, never by fading content in: a Rectangle's and an Image's opacity
    reliably blend, a VideoOutput's does not. The cover is the last child,
    hides the whole stack until there is something to show, and dissolves away
    when there is. Verified against a bright moving test video: the transition
    is a bell-shaped cross-dissolve over ~11 frames, the signature of two
    different images blending, where a cut would be one frame at full
    difference with nothing either side.
  * The reveal also waits for the video's first frame to have been painted
    rather than merely delivered, and for the cover to finish dissolving before
    the clock is allowed to arrive, so the glass letterforms never sample a
    half-dissolved backdrop. Poster and cover now key off the sink's
    videoFrameChanged instead of the playback state, which is reached before
    anything is on screen.
  * Static images no longer fade in on their own; the cover's dissolve covers
    that case and the video case with one mechanism.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 20:15:00 +0800

lumina-lock (0.4.10-1) unstable; urgency=medium

  * Fixed the lock screen flashing its content on every re-lock: the clock
    appeared at full opacity, faded out, then faded back in. The surfaces stay
    alive between locks, so when a re-lock resets the scene the clock was
    still at the opacity the previous lock left it at. resetForLock() clears
    `animating` precisely so the layout snaps back to Idle instead of
    animating, but the clock's entry reveal and the hint's reveal did not
    honour it: they animated the *hide* with the same 480 ms fade that is
    meant for the reveal, while the window was being shown at the same time.
    Both are now gated on `animating`, like the transition properties already
    were.
    Measured over a lock -> unlock -> lock cycle, the clock region used to run
    97 -> 95/89/87/89/93/96/97 (down, then back up) once the window reappeared;
    it now holds the wallpaper's own brightness and rises monotonically once.
  * The cold-start path never showed this: a plain launch starts already
    locked, so resetForLock() is never called. Only the resident daemon
    (`--daemon`, the mode the package installs) reaches it, on the second and
    every later lock.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 20:05:00 +0800

lumina-lock (0.4.9-1) unstable; urgency=medium

  * Fixed the lock screen's own entry. The clock and date used to show up
    small and fully opaque, then grow while apparently losing and regaining
    their opacity. Two independent causes, both removed:
    - The type size is now animated only once the scene has settled. `unit`
      is derived from the window height, which is 0 until the window has been
      sized, so the first layout animated the size up from nothing on every
      lock. Until sceneReady the size snaps instead of animating.
    - The glass text's opaque fallback no longer keys off `sampling`, which
      is also false for the first frame or two before the item has been laid
      out; that flashed an opaque white clock over the wallpaper until the
      glass replaced it. The fallback now appears only when the glass
      genuinely cannot be rendered, which is a persistent condition. The
      entry is a single 480 ms fade at a constant size.
  * The video wallpaper no longer cuts from its poster to the moving frame.
    The poster hid itself within a single frame of the video starting; it now
    cross-fades over 500 ms, so the still cover dissolves into the wallpaper.
    The poster's visibility follows its own opacity, which keeps it alive for
    the length of the fade.
  * Every fade now goes through the shared MotionBehavior: the clock's entry
    reveal, the static wallpaper's fade-in, the poster dissolve and the inline
    error each still carried their own Behaviour and their own curve.
    MotionBehavior takes an optional duration, so the curve lives in one place
    and only the length varies by use.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 19:20:00 +0800

lumina-lock (0.4.8-1) unstable; urgency=medium

  * Clock and date font size are configurable: 80-240 and 14-48, expressed in
    reference pixels at a 1080px-tall screen and scaled to the actual
    resolution, set from the control center with a spin box next to each
    weight row. The authenticating sizes stay a fixed fraction of the idle
    ones, so a custom size keeps the same relationship between the two states.
    Bounds are enforced in C++ so a hand-edited config cannot shrink the clock
    into illegibility or push the date off the screen.
  * Retuned the transition curve. Every state-transition animation now shares
    one curve, a standard ease-in-out cubic-bezier(0.4, 0, 0.2, 1), replacing
    Easing.OutCubic: OutCubic spent ~58% of the motion in the first quarter of
    the time, which reads as "jump, then creep" when a large element both moves
    and resizes. Measured on the scene dim, the new curve is at 8% of the
    change after 34 ms and 40% after 118 ms, where OutCubic would be at 28% and
    75%. The curve is declared once in Theme and applied through a shared
    MotionBehavior component, so the call sites cannot drift apart again.
    Note for future edits: the curve must be set through `easing.bezierCurve`,
    not `easing.type` — the type setter builds a fresh QEasingCurve and would
    discard the control points.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 18:10:00 +0800

lumina-lock (0.4.7-1) unstable; urgency=medium

  * Synchronise the authentication transition. `AuthView.reveal` is bound to
    `content.authReveal` and *both* had a 320 ms Behavior, so the value was
    smoothed twice and the password field lagged a whole extra duration behind
    the clock — visible both when the field appears and when Escape returns to
    the idle state. The panel now follows the scene's already-animated value
    with no Behavior of its own, so one animation drives the whole transition.
    Measured at 60 fps: clock and pill now start on the same frame and settle
    on the same frame, in both directions.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 17:20:00 +0800

lumina-lock (0.4.6-1) unstable; urgency=medium

  * Date animation no longer steps. QFont::setPixelSize takes an int, so
    animating font.pixelSize made the 22px date snap through four visible sizes
    across the transition while the 125px clock took forty-five and read as
    continuous — measured, the date's width moved in three 11px jumps where the
    clock's took seven finer ones. GlassText now animates a continuous `size`,
    rasterises the glyphs at the nearest integer size and applies a sub-pixel
    scale correction, so the motion interpolates smoothly and the glyphs stay
    crisp. Side effect: the date now renders at its designed 27*unit instead of
    being truncated to 22px, so it is ~1px larger in the idle state.
  * Entry no longer stutters. The full-screen blur was pre-warmed by drawing it
    for the first 12 rendered frames; because the blur chain runs even at amount
    0, that cost a full-screen multi-level blur per frame and the entry
    animation lost frames (a plateau mid-fade, measured). It is now warmed once,
    after the entry has settled, where the cost is invisible.
  * Settings page rewritten to the control-center conventions: every row carries
    backgroundType: DccObject.Normal, values moved into the row description, DTK
    flat combo boxes and a section title above the clock rows. Row heights,
    button sizes and paddings are left at the control center's defaults rather
    than overridden per item.
  * File pickers now open the *system* file dialog instead of the Qt Quick
    built-in one (DontUseNativeDialog is gone), matching every other
    control-center plugin.
  * The unlock hint fades and rises into place, then breathes.
  * A printable key that wakes the lock now becomes the first character of the
    password instead of being swallowed.
  * Password field: the dots are centred, each new character gives a short pop,
    a wrong password bounces (decaying sideways shake plus a springy dip rather
    than a flat slide) and a correct one flashes the accent colour and pulses
    outward before the scene fades.

 -- Lumina <lumina@example.com>  Tue, 15 Sep 2026 15:00:00 +0800

lumina-lock (0.4.5-1) unstable; urgency=medium

  * Fix the frozen start of the first Idle -> Authenticating transition in a
    lock process. The full-screen wallpaper blur was created with
    visible/blurEnabled off and switched on only when the transition begins, so
    its first render — compiling the level-3 blur shader and allocating the
    full-resolution multi-level FBO chain — landed inside the animation.
    Measured by recording the transition at 60 fps: 9 identical frames before,
    none after.
    The effect's blur items are now enabled from startup and it is drawn (at
    amount 0, which is a plain copy of the wallpaper) for the first frames the
    window renders, which is what actually compiles the shader. It is then
    hidden again: a video wallpaper dirties the scene every frame, and leaving
    the blur drawn permanently cost ~2.9 cores of software rasterisation on
    every idle frame (measured 483% vs 199% of one core).
    The warm-up counts rendered *frames*, not wall-clock time — the window's
    first paint can land later than any timer window would allow for, and a
    warm-up that ends before the first frame never draws the effect at all.
  * README: the transition description no longer claims the video is frozen
    during the exit (that was removed in 0.4.2).

 -- Lumina <lumina@example.com>  Mon, 14 Sep 2026 15:10:00 +0800

lumina-lock (0.4.4-1) unstable; urgency=medium

  * Pull the frosted glass edge softening back from ~4px to ~1.5px. 0.4.3
    removed the stair-stepping but at the cost of visibly out-of-focus type.
    Antialiasing at a fixed output resolution is a one-pixel ramp that no
    amount of supersampling can widen (rendering at 2x and scaling down just
    re-derives the same per-pixel coverage), so smoothing a large display
    glyph beyond its own coverage is inherently a blur. The chosen amount
    drops the steepest edge steps by ~20% while keeping the letterform crisp;
    GlassText.maskSoftness is the single knob.

 -- Lumina <lumina@example.com>  Mon, 14 Sep 2026 11:45:00 +0800

lumina-lock (0.4.3-1) unstable; urgency=medium

  * Frosted glass is now antialiased for real. The previous fix only shaped
    the mask's alpha ramp; the remaining stair steps came from the mask
    textures being consumed at 1:1, where a curve's edge can only ever be one
    pixel wide. The masks are now filtered (layer.smooth) and softened by a
    blur applied *inside* the mask layer.
    Note for future edits: a `layer.effect` on a mask item silently does
    nothing — Qt applies a layer effect while nesting the layer into the
    scene, and the mask item is never drawn — and the mask blur must use
    blurMax <= 16, because at blurMax 32 the blur runs at quarter resolution
    and the amount quantises into coarse steps (0.08 and 0.16 rendered
    identically).
  * Rounded shapes (the mask, the pill's fill and its 1px hairline) request
    antialiasing.
  * The password placeholder uses a stronger text colour, now that the pill
    itself is dimmed while authenticating.

 -- Lumina <lumina@example.com>  Mon, 14 Sep 2026 10:30:00 +0800

lumina-lock (0.4.2-1) unstable; urgency=medium

  * Antialias the frosted glass. The mask was sampled through a hard alpha
    threshold, which discarded the text renderer's antialiasing and left
    jagged glyph strokes and jagged rounded corners; the mask now uses a
    smooth alpha ramp.
  * The clock's font-size transition animates again. The type is now driven by
    scalar font properties instead of an alias to the child Text's grouped
    `font`, which a Behavior cannot target — that alias turned the Idle →
    Authenticating transition into a hard switch. All scene transitions also
    share one duration and easing, so the clock, the authentication panel and
    the background dimming move as a single gesture.
  * The password field no longer stays bright while the rest of the scene dims
    during authentication: the glass mirrors the scene dimming, which it does
    not inherit because it samples the wallpaper rather than the composed
    scene.
  * Unlocking no longer pauses a video wallpaper — playback continues through
    the exit fade.

 -- Lumina <lumina@example.com>  Thu, 11 Sep 2026 19:40:00 +0800

lumina-lock (0.4.1-1) unstable; urgency=medium

  * The clock is now frosted-glass type, not text on a glass card: the glyphs
    act as a mask so the blurred wallpaper shows through the letterforms (the
    iOS lock-screen look), with a glyph-shaped shadow so the time stays
    readable over any wallpaper. The password field keeps its rounded glass
    panel. The sample is clipped to the glyph/panel shape, so no bright
    rectangle leaks out while the scene above the wallpaper is dimmed.
  * Blur video wallpapers too. The glass used to be disabled for video
    wallpapers on the assumption that live video frames could not be captured
    into a shader; in practice sampling the VideoOutput works, so video now
    gets the same frosted look. Video samples every frame, static wallpapers
    stay on the on-demand path.

 -- Lumina <lumina@example.com>  Thu, 11 Sep 2026 19:20:00 +0800

lumina-lock (0.4.0-1) unstable; urgency=medium

  * Lock-screen polish:
    - Frosted glass behind the clock/date and the password field: each panel
      samples only the wallpaper region it covers, blurs it and clips it to a
      rounded shape, so an idle lock screen does no per-frame blur work. Falls
      back to a translucent surface on video wallpapers and on software
      scene-graph backends.
    - Clock and date font weight are configurable (thin … bold) via DConfig
      and the control-center plugin.
    - Busy indicator is now an arc covering a fifth of the ring instead of a
      single travelling dot.
    - Unlock transition: the video decoder is frozen before the fade starts,
      content fades before the window, and the per-frame content scaling is
      gone — that scaling of the large clock type was the main source of
      stutter, worst right after the wallpaper had loaded.
    - The clock now fades in on a short beat after lock (and once the
      wallpaper can draw), instead of popping in over a half-decoded image.
  * Keyboard and pointer are grabbed while locked (X11), so window-manager
    shortcuts such as Alt+Tab can no longer switch away from the lock; the
    grab is released on unlock.
  * Package now depends on qml6-module-qtquick-effects and
    qml6-module-qtquick-shapes (and, since the previous build,
    qml6-module-qtquick-dialogs); the control-center plugin uses Qt Quick
    dialogs instead of QFileDialog, which aborted the control center because
    it runs on QGuiApplication.

 -- Lumina <lumina@example.com>  Thu, 11 Sep 2026 18:40:00 +0800

lumina-lock (0.3.0-1) unstable; urgency=medium

  * Add control-center wallpaper configuration: a dde-control-center plugin
    (plugins_v1.1/luminalock) edits the org.lumina.lock DConfig keys
    (wallpaperType / wallpaperPath / videoPath / posterPath), and the lock
    applies them on startup and live via DConfig change notifications. CLI
    --wallpaper / --video still override the configured value.
  * Drop-in dde-lock replacement for DDE: /usr/bin/dde-lock is taken over via
    dpkg-divert (dde-session-shell stays installed, lightdm greeter untouched,
    original wrapper restored on removal); the org.deepin.dde.LockFront1
    D-Bus surface (Show / ShowAuth / Suspend / Hibernate / Visible) matches
    dde-lock, and the Power1 SleepLock setting is honoured on suspend resume.
  * X11 lock windows carry _DEEPIN_LOCK_SCREEN / _DEEPIN_NET_STARTUP and use
    dde-lock's window flags so the deepin window manager keeps them on top and
    focused.
  * A systemd user drop-in adapts dde-session's dde-lock.service
    (Type=simple, ExecStart=/usr/bin/dde-lock --daemon --pam-service dde-lock)
    to this non-forking process.

 -- Lumina <lumina@example.com>  Thu, 10 Sep 2026 20:30:00 +0800
